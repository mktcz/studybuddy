name: ui
description: "StudyBuddy shared UI system — tokens, theme and components for the phone and watch apps."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.13.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
